# Copy to .env — that file is gitignored.
GROQ_API_KEY=

# Optional. A registry key (gpt-oss, gpt-oss-20b, qwen, llama) or any Groq model id.
# Defaults to gpt-oss. See: python app.py --list-models
# SYSTEM_EXPERT_AGENT_MODEL=qwen
