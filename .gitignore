# Rust
/target/
**/target/
# Cargo.lock is committed since this workspace produces a binary (system-expert-tauri-ui)

# Tauri / frontend
crates/system-expert-tauri-ui/node_modules/
crates/system-expert-tauri-ui/dist/

# Python agent
agent/.venv/
agent/__pycache__/
agent/**/__pycache__/
agent/*.egg-info/
# LangGraph conversation checkpoints (per-machine, not the app's chat transcript)
agent/checkpoints.sqlite*

# Secrets — the agent reads its API key from the environment
.env
*.env
!.env.example

# Editor / OS
.vscode/
.DS_Store
.env